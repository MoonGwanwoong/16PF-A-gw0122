1. 함수를 선언하면 호출해야 오류가 발생하지 않는다(거짓)
6. if문을 위해 반드시 필요한 것
if, 조건문, :, 들여쓰기된 내용
elif와 else는 없어도됨

2. else문은 하나만 가능하다(else는 여집합)
3. if문안에 다른 if elif else문 포함시키는것이 가능하다
4. 정해진 횟수만큼 특정코드 실행 - for(ex36참고)
5. 조건에 따라 특정코드 반복실행 - while
6. 처리대상 문자열 목록이 있을경우 - for
7. 사용자로부터 특정 입력을 받을때까지 무한 반복 - while

3. 함수로 분할하는 프로그래밍 - 구조적 프로그래밍
4. 되도록 for문을 쓴다 die를 만든다
5. **= 기호의 이름 : power assign, ** : 제곱
6. pop : 제일 뒤의것이 나옴
8. append는 맨 뒤에 추가해주는것
9. stuff[-2] : 뒤에서 두번째
10. list는 순서로 구별[] dictionary는 key로 구별{}
    list를 dic의 key로 쓸 수 없다(괄호를 소괄호로 바꿔주면 가능)
    []와 () - 튜플 차이가 있음 리스트는 순서를 가림
    튜플은 딕에서 키로 쓸수있음
    list를 dic의 value로 쓸 수 있음
    dic의 key는 항상 정렬되어있지 않음